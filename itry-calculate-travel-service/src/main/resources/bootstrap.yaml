spring:
  application:
    name: itry-calculate-travel-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config:8888